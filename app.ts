function add(num1: number, num2: number, showResult: boolean, phrase: string) {
  return num1 + num2;
}

const n1 = 20;
const n2 = 15;
const printResult = 8;
const aWord = "dog";

const sum = add(n1, n2, printResult, aWord);
console.log(sum);
